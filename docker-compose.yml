version: '3'
services:
  web:
    build: ./
    container_name: johnstratoudakis/ontheroad:latest
    ports:
#     - "5000:5000"
      - "5080:80"
#   environment:
#     - FLASK_APP=main.py
#     - FLASK_DEBUG=0
#     - 'RUN=flask run --host=0.0.0.0 --port=5000'
#   command: flask run --host=0.0.0.0 --port=5000

